export const changeKey = (key) => {
    return {
        type: 'CHANGE_KEY',
        key: key
    }
}

export const changeMode = (mode) => {
    return {
        type: 'CHANGE_MODE',
        mode: mode
    }
}